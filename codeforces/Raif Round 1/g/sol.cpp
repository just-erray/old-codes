// author: erray
#include<bits/stdc++.h>
 
using namespace std;
 
int main () {
  ios_base::sync_with_stdio(false);
  cin.tie(0);
  int k;
  cin >> k;
  vector<int> f(6);
  for (int i = 0; i < 6; ++i) {
    cin >> f[i];
  }
  int q;
  cin >> q;
  while (q--) {
    int n;
    cin >> n;
    
  }
}