// author: erray
#include<bits/stdc++.h>
 
using namespace std;
 
int main () {
  ios_base::sync_with_stdio(false);
  cin.tie(0);
  int n, m;
  cin >> n >> m;
  vector<int> x(m), y(m), w(m);
  for (int i = 0; i < m; ++i) {
    cin >> x[i] >> y[i] >> w[i];
  }

  
}