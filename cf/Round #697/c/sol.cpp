// author: erray
#include<bits/stdc++.h>
 
using namespace std;
 
int main () {
  ios_base::sync_with_stdio(false);
  cin.tie(0);
  int T;
  cin >> T;
  while (T--) {
    int n, m, k;
    cin >> n >> m >> k;
    vector<int> a(k), b(k);
    for (int i = 0; i < k; ++i) {
      cin >> a[i];
      --a[i];
    }

    for (int i = 0; i < k; ++i) {
      cin >> b[i];
      --b[i];
    }

    long long ans = 0;
    
    for (int i = 0; i < k; ++i) {
      
    }
  }
}