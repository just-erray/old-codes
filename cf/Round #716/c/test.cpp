// author: erray
#include <bits/stdc++.h>
 
using namespace std;
 
int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(0);
  int n;
  cin >> n;
  for (int i = n; i >= 1; --i) {
    ofstream out("int");
    out << i;
    system("sol <int");
  }
}