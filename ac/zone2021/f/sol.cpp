// author: erray
#include <bits/stdc++.h>
 
using namespace std;
 
int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(0);
  int n, m;
  cin >> n >> m;
  vector<bool> out(n);
  for (int i = 0; i < m; ++i) {
    int x;
    cin >> x;
    out[x] = true;
  }
  for (int i = 0; i < n; ++i) {
    
  }
}